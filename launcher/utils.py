"""
Shared utility functions for the CONJURE platform.
"""

import json
from pathlib import Path

def load_json(file_path: Path) -> dict:
    """Load a JSON file."""
    pass

def save_json(data: dict, file_path: Path) -> None:
    """Save data to a JSON file."""
    pass 